name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -name: Clonar
      uses: actions/checkout@v2

    -name: Mostrar Estructura
    run: ls - R
