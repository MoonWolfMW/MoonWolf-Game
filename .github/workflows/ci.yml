name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    stepst:
      -use: actions/checkout@v2
      -name: Fin
      run: echo "Solo revision basica...Soon"
